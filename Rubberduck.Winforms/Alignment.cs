namespace Rubberduck.Winforms
{
    /// <summary>
    /// Specifies where the <see cref="Label"/> will be rendered in relation to the control that's being validated.
    /// </summary>
    public enum Alignment
    {
        Top,
        Bottom,
        Right,
        Left
    }
}